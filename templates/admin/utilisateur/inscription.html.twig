{% block stylesheet %}

    <link rel="stylesheet" href="https://stackpath.bootstrapcdn.com/bootstrap/4.5.2/css/bootstrap.min.css"
          integrity="sha384-JcKb8q3iqJ61gNV9KGb8thSsNjpSL0n8PARn9HuZOnIxN0hoP+VmmDGMN5t9UJ0Z"
          crossorigin="anonymous">

{% endblock %}

    {% block body %}
        <div class="container animate__animated animate__backInDown" style="width: 60%">
            {% if editMode %}
                <h1 class="text-left">Modification utilisateur</h1>
            {% else %}
                <h1 class="text-left">Inscription utilisateur</h1>
            {% endif %}

            {{ form_start(form) }}

            {{ form_row(form.pseudo) }}
            {{ form_row(form.nomComplet) }}
            {{ form_row(form.fonction) }}
            {{ form_row(form.societe) }}
            {{ form_row(form.plainMdp) }}

            <button type="submit" class="btn btn-primary">
                {% if editMode %}
                    Mettre à jour
                {% else %}
                    Enregistrer
                {% endif %}
            </button>

            {{ form_end(form) }}
        </div>
    {% endblock %}
