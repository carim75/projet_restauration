{% extends 'base.html.twig' %}



{% block body %}

    <main class="main px-4 py-5" style="width: 60%">

        <ul class="nav nav-tabs"id="media">
            <li class="nav-item">
                <a  class="nav-link" href="{{ path('app_index_homepage') }}">Accueil</a>
            </li>
            <li class="nav-item">
                <a  class="nav-link" href="{{ path('app_index_livraison') }}">Livraison</a>
            </li>
            <li class="nav-item">
                <a  class="nav-link" href="{{ path('app_index_listecommande') }}">Commandes</a>
            </li>
            <li class="nav-item">
                <a  class="nav-link" href="{{ path('app_index_listeproduit') }}">Liste des produits</a>
            </li>
            <li class="nav-item">
                <a  class="nav-link active" href="{{ path('app_index_editproduit') }}">Ajout/Suppression de produit</a>
            </li>
            <li class="nav-item">
                <a  class="nav-link" href="{{path('app_index_promos')}}">Promotions</a>
            </li>
        </ul>
        <h1 class="mt-4 text-center">Création produit</h1>
        <hr>
        {{ form_start(FormProduit) }}
        {{ form_row(FormProduit.nom,{'label': 'Nom du produit', 'attr': {'placeholder' : 'Nom du produit'}}) }}
        {{ form_row(FormProduit.categorie,{'label': 'Catégorie'}) }}
        {{ form_row(FormProduit.photo) }}
        {{ form_row(FormProduit.prix,{'label': 'Prix', 'attr': {'placeholder':'Prix'}}) }}
        {{ form_row(FormProduit.unite,{'label': 'Unité'}) }}
        {{ form_row(FormProduit.societe) }}
        {{ form_row(FormProduit.description,{'label': 'Description', 'attr':{'placeholder' : 'Origine, Conditionnement, si possibilité de colis renseigner poids moyen ou quantité...' }}) }}
        {{ form_row(FormProduit.promotion,{'label' : 'Promotion'}) }}
        <!--<button class="btn btn-primary" type="submit">Enregistrer</button>-->
        <input type="submit" value="Enregistrer" class="btn btn-dark btn-lg btn-block mt-4">

        {{ form_end(FormProduit) }}


    </main>
{% endblock body %}